[Unit]
Description=BuildBot master
After=network.target

[Service]
User={{ buildbot_user }}
Group={{ buildbot_user }}
WorkingDirectory={{ buildbot_base }}
ExecStart={{ buildbot_base }}/bin/buildbot start --nodaemon

[Install]
WantedBy=multi-user.target
