[Unit]
Description=BuildBot worker service
After=network.target

[Service]
User={{ buildbot_user }}
Group={{ buildbot_user }}
WorkingDirectory={{ buildbot_base }}
ExecStart={{ buildbot_base }}/bin/buildbot-worker start --nodaemon

[Install]
WantedBy=buildbot.service
